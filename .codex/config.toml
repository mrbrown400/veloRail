# VeloRail repo-scoped Codex defaults.
#
# Keep repo-specific operating rules in AGENTS.md and .agents/skills/.
# Do not add MCP servers, code indexes, databases, or orchestration runtimes here
# unless a Linear issue and the harness-curation workflow explicitly approve them.

model = "gpt-5.5"
model_reasoning_effort = "xhigh"
project_doc_max_bytes = 65536

[features]
multi_agent = true
